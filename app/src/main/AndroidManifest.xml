<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newsha.decisonmaker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Options" />
        <activity android:name=".Factors" />
        <activity android:name=".Factor1" />
        <activity android:name=".Factor2" />
        <activity android:name=".Factor3" />
        <activity android:name=".Factor4" />
        <activity android:name=".Factor5"></activity>
    </application>

</manifest>